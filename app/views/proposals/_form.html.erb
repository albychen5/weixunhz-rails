<%= simple_form_for(@proposal) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :info %>
    <!-- Add in a collection select for the group this proposal goes to -->
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
