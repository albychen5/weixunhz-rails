<div class="row">
	<div class="col-xs-12 col-md-8 col-md-offset-2">
    <div class="panel panel-default">
			<div class="panel-body text-center">
				<span class="section-title">Editing Proposal for <%= @proposal.name %></span>
			</div>
		</div>
		<%= render 'form' %>
	</div>
</div>

<div class="text-center">
<%= link_to 'Back', :back %> |
<%= link_to 'Delete', @proposal, method: :delete, data: { confirm: 'delete this proposal?' } %>
</div>