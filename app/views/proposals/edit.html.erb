<div class="row">
	<div class="col s12 m8 offset-m2">
		<div class="card">
			<div class="card-content">
				<span class="card-title">Editing Proposal for <%= @proposal.name %></span>

				<%= render 'form' %>

			</div>
		</div>
	</div>
</div>
<div class="center-align">
<%= link_to 'Back', :back %> |
<%= link_to 'Delete', @proposal, method: :delete, data: { confirm: 'delete this proposal?' } %>
</div>